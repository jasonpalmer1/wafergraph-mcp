/** Single source for the Worker/MCP advertised version. Keep in sync with package.json + server.json (`npm run check:versions`). */
export const PACKAGE_VERSION = "1.3.6";

/** Registered tool count advertised on the landing page. Bump when adding/removing tools. */
export const TOOL_COUNT = 36;
